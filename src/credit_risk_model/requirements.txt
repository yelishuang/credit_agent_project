# credit_risk_model 依赖
# Python >= 3.10
#
# 模型加载（joblib 序列化的 LGBMClassifier 需要 scikit-learn）
lightgbm>=4.0,<5
scikit-learn>=1.4,<2
joblib>=1.3

# 特征工程（Polars 读取/过滤 parquet，pyarrow 做 to_pandas 转换）
polars>=1.0,<2
pyarrow>=15.0

# 数据处理
pandas>=2.0,<3.1
numpy>=1.26,<3
